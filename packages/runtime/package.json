{
  "name": "@webmcp/runtime",
  "version": "0.1.0",
  "description": "Browser-side WebMCP runtime — provides window.mcp.registerTool()",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "rimraf": "^6.0.0"
  },
  "keywords": ["webmcp", "mcp", "ai-agent", "browser-tools"],
  "license": "MIT"
}
