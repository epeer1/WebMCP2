{
  "name": "webmcp-instrument-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "build:engine": "npm run build -w webmcp-instrument-engine",
    "build:runtime": "npm run build -w webmcp-instrument-runtime",
    "build:server": "npm run build -w webmcp-instrument-server",
    "build:cli": "npm run build -w webmcp",
    "dev:server": "npm run dev -w webmcp-instrument-server",
    "dev:cli": "npm run dev -w webmcp",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "playwright": "^1.58.2",
    "prettier": "^3.5.0",
    "turbo": "^2.4.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "packageManager": "npm@10.0.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
